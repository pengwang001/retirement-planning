services:
  - type: web
    name: retirement-planner
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn retirement_app:app --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0
      - key: PORT
        value: 10000
